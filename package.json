{
  "name": "glitch-gremlin", // Or whatever your root project's name is
  "version": "1.0.0",
  "workspaces": [
    "sdk"
  ],
  "private": true, // Add this if you don't want to publish the root package
  "scripts": {
    "build": "npm run build --workspace=@glitch-gremlin/sdk"
  }
}
